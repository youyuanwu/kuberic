# Canonical KinD cluster config for all xedio workloads.
# No NodePort mappings — use kubectl port-forward for test access.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
